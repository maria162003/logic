allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

// Firebase dependencies
buildscript {
    dependencies {
        classpath 'com.google.gms:google-services:4.4.0'
    }
}
